[project]
name = "engine"
version = "0.1.0"
description = "Django backend for search project"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.3",
    "django-htmx>=1.27.0",
]

[project.optional-dependencies]
dev = [
    "django-extensions",
]
