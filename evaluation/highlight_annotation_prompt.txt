You are a data annotation engineer.

Task:
For each query and each retrieved transcript chunk, annotate the MOST IMPORTANT parts of the chunk with respect to the query.

Output format (strict):
<query>
<show name>
<episode name>
<highlight_1 || highlight_2 || ... || highlight_n>
<\n>

Rules:
1. Copy highlights EXACTLY from the chunk text (no paraphrasing).
2. Each highlight should be a short phrase (roughly 5-15 words).
3. Add 1-3 highlights when the chunk contains useful evidence for the query.
4. If the chunk has no important part for the query, write exactly: NONE
5. Keep the same result order as in query_results.txt.
6. Do not add extra fields, numbering, or commentary.

Quality examples:
- Good: "millions of people under lockdown"
- Good: "declaring a public health emergency"
- Bad: paraphrased text not present in chunk
- Bad: full sentence blocks that are too long

Create a single file named: annotated_highlights.txt
